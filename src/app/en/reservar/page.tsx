// Versión en inglés del catálogo del motor de reservas. Reutiliza la MISMA
// plantilla; el locale ("en") lo resuelve la página vía el header x-locale que
// pone el middleware.
export { default, generateMetadata } from "../../reservar/page";
