// Versión en inglés del listado de destinos. Reutiliza la MISMA página; el locale
// ("en") lo resuelve vía el header x-locale del middleware.
export { default, generateMetadata } from "../../destinos/page";
