// Versión en inglés de la página de destino. Reutiliza la MISMA plantilla; el locale
// ("en") lo resuelve la plantilla vía el header x-locale del middleware.
export { default, generateMetadata, generateStaticParams } from "../../../destinos/[slug]/page";
