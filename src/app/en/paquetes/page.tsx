// Versión en inglés del listado de paquetes. Reutiliza la MISMA plantilla; el
// locale lo resuelve la página vía el header x-locale del middleware.
export { default, generateMetadata } from "../../paquetes/page";
