// Versión en inglés del checkout de paquete. Es la misma pantalla: el componente
// resuelve el idioma con `useLocale()`, que lo deduce de la ruta.
export { default } from "../../../reservar-paquete/[slug]/page";
