// Versión en inglés de la pantalla de éxito. A esta URL llega el `return_url` de
// Stripe y el `router.push` del carrito, ambos ya con prefijo de idioma.
export { default } from "../../../reservar-tour/confirmacion/page";
